<%= form_for(@task) do |f| %>
  <%= f.label :date %>
  <%= f.date_field :date %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :done %>
  <%= f.check_box :done %>

  <%= f.submit %>
<% end %>
